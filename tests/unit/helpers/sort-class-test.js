import { sortClass } from '../../../helpers/sort-class';
import { module, test } from 'qunit';

module('Unit | Helper | sort class');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = sortClass(42);
  assert.ok(result);
});
